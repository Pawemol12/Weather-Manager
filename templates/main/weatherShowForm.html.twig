<div class="form-wrapper">
    {{ form_start(weatherShowForm) }}
    <div class="row">
        <div class="col">
            {{ form_row(weatherShowForm.city) }}
        </div>
        <div class="col">
            {{ form_row(weatherShowForm.show, {'attr': {'class': 'btn-block btn btn-primary searchFormBtn'}}) }}
        </div>
    </div>
    {{ form_end(weatherShowForm) }}
</div>