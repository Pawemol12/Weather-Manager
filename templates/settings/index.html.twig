{% extends 'base.html.twig' %}
{% set title='settings.title'|trans %}
{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h2>{{ title }}</h2>
            <hr/>
        </div>
    </div>

    <div class="row">
        <div class="col">
            {% include 'snippets/alertContainer.html.twig' %}
        </div>
    </div>

    <div class="row">
        <div class="col">
            <div id="SettingsFormWrapper">
                {% include 'settings/settingsForm.html.twig' %}
            </div>
        </div>
    </div>

{% endblock %}
